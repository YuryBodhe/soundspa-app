# SoundSpa v2 handoff — 2026-09-04 14:31 UTC+07

## 1. READ THIS FIRST

This operational checkpoint is intended for a completely fresh AI session. It was updated later on the same work session/day after real staging verification of MP3 exhaustion recovery.

Before doing any work:

1. Work only in `/Users/bodhem3/Zavod/apps/soundspa-v2` unless the user explicitly changes scope.
2. Check `git status`, branch, HEAD, and any current diff.
3. Expected branch: `soundspa-v2`. Latest staging-verified checkpoint: `a1b60a348ebbdf1d7b9f5c94321c91bc01c3df5`.
4. Do not discard, stage, commit, push, deploy, or rewrite anything without explicit approval.
5. Never touch production, Timeweb, Docker, nginx, DB, DNS, or media unless explicitly requested.

Status labels:

- **VERIFIED ON REAL STAGING** — observed in a reported real browser/network staging test.
- **VERIFIED LOCALLY** — confirmed from Git, code, or local checks.
- **IMPLEMENTED BUT FAILED REAL STAGING VERIFICATION** — committed, but failed the reproduced staging case.
- **IMPLEMENTED BUT NOT FULLY VERIFIED** — present but not proven in every relevant real-browser scenario.
- **KNOWN LIMITATION** — accepted current boundary.

## 2. Project, paths, and isolation

- GitHub repository: `YuryBodhe/soundspa-app`
- SoundSpa v2 worktree: `/Users/bodhem3/Zavod/apps/soundspa-v2`
- Original/legacy checkout: `/Users/bodhem3/Zavod/apps/soundspa-app`
- Branch: `soundspa-v2`
- Remote: `origin` → `git@github.com:YuryBodhe/soundspa-app.git`
- V2 route: `/v2`
- Staging URL: `https://test.soundspa.bodhemusic.com/v2`
- Production path: `/var/www/soundspa`
- Isolated v2 staging path: `/var/www/soundspa-v2`
- Staging bind: `127.0.0.1:3101`; container port: `3000`
- Staging compose: `docker-compose.staging.yml`, app service only, dummy `DATABASE_URL` for legacy build-time imports.
- Production stable rollback baseline: `7741cc2`.
- Production remains SoundSpa v1/AzuraCast for active salons and is **UNCHANGED**. Never describe production as running these v2 commits.

Normal workflow:

```text
local change → review/checks → approved commit → approved push
→ separately approved isolated staging deploy → real-browser test
```

Production migration/deployment is a separate future decision.

## 3. Current architecture

### V2 boundary — VERIFIED LOCALLY

- UI: `app/v2/V2Player.tsx`
- Music: `app/lib/audio/mp3Engine.ts`
- Ambient: `app/lib/audio/ambientEngine.ts`
- Playlist: `app/v2/divnitsaPlaylist.ts`
- Mock channels: `app/v2/mockData.ts`
- V2 is one responsive `/v2` page and does not use legacy players or `soundEngine`.
- It currently has no salon auth, DB catalog, device activation, entitlements, Prodamus, monitoring heartbeat, or AzuraCast playback.
- Music and ambient have independent engines and `HTMLAudioElement`s.

### Music engine

Current hybrid progressive-network plus Blob-cache behavior:

1. A network-backed `HTMLAudioElement` provides progressive HTTPS playback.
2. Startup waits for 5 contiguous buffered media-seconds.
3. Public `playing` requires genuine `currentTime` progression.
4. Buffer health is measured from the buffered range containing `currentTime`.
5. Audible network traffic has priority over full-track fetching.
6. Complete raw music Blobs are stored on demand in a shared, bounded `MusicSessionCache`; each active engine creates its own Blob URLs and uses two reusable A/B playback references.
7. `desiredNextIndex` stays independent from the Blob currently used as fallback.
8. Fetch lifecycle uses AbortController, timeout, request/generation validation, retry backoff, and URL cleanup.
9. Pause preserves position and cached Blobs; since `76dc20f`, authorized A/B preparation may continue while paused.
10. A complete same-track Blob is preferred when progressive playback becomes unusable.
11. Without a usable Blob, a confirmed-dead network Audio is recreated, the intended position restored, startup buffering repeated, and playback resumed automatically.

Divnitsa URLs:

- `/music/divnitsa/divnitsa-mix-01.mp3`
- `/music/divnitsa/divnitsa-mix-02.mp3`
- `/music/divnitsa/divnitsa-mix-03.mp3`

Relax is also a real music channel and uses:

- `/music/relax/relax-mix-01.mp3`

Divnitsa and Relax use the same verified `Mp3Engine`, with only one music engine active at a time. `432 Hz` remains UI-only/planned.

### Critical recovery invariant

The following are **not** definitive recovery success:

- `audio.buffered` availability or growth;
- `canplay`;
- `playing` event;
- resolved `audio.play()` Promise.

An exhausted Audio may retain/grow buffered ranges while remaining frozen. Recovery succeeds only after observable genuine `currentTime` progression.

### Ambient — VERIFIED ON REAL STAGING

Commit `7a30ffb` provides progressive first playback and background full-Blob preparation for only the selected ambient. Completed sounds use a maximum-three in-memory LRU; the active entry is protected, duplicates/stale switches are guarded, and a prepared Blob takes over at a later natural loop boundary. Music and ambient remain independent.

URLs: `/noise/forest.mp3`, `/noise/night.mp3`, `/noise/sea.mp3`.

### Media storage

Intended server storage is outside Git/application image:

```text
/var/lib/soundspa-v2-media/
  music/divnitsa/divnitsa-mix-01.mp3
  music/divnitsa/divnitsa-mix-02.mp3
  music/divnitsa/divnitsa-mix-03.mp3
  music/relax/relax-mix-01.mp3
  ambient/forest.mp3
  ambient/night.mp3
  ambient/sea.mp3
```

Logical URLs do not expose that physical path. Historical caveat: ambient files under `public/noise/` were already tracked despite ignore rules. Do not alter media tracking/packaging without a separate approved task.

## 4. Exhaustion recovery history

### Failed checkpoint — `8734a63`

```text
8734a63
fix: recover MP3 playback after network exhaustion
```

Status: **IMPLEMENTED BUT FAILED REAL STAGING VERIFICATION**.

- Internet returning before buffered media exhausted allowed natural recovery.
- After full buffer exhaustion, restoring internet did not recover playback.
- Failure reproduced with both Wi-Fi and mobile tethering.

Never present `8734a63` as a successful fix.

### Diagnostic checkpoint — `9ff2ae2`

```text
9ff2ae2884dafbd0518ca72bb1c0d97a63233476
chore: instrument MP3 recovery diagnostics
```

Prefix: `[Mp3Recovery]`.

The trace showed the old Audio reporting:

```text
currentTime = 36.290339
bufferAhead ≈ 58.93 seconds
sourceVersion = 1
```

while `currentTime` stayed permanently frozen. It repeatedly entered:

```text
startup-buffer-ready → handoff-grace → network retry scheduling
```

without `old-audio-destroyed`, `new-audio-created`, or `real-progression-restored`.

Confirmed cause: buffer availability/growth is not proof that an exhausted Audio can recover audibly.

### Verified core fix — `d82e39f`

```text
d82e39faa251d7ca31e3ded8f4c406a091db945a
fix: recreate dead MP3 source after exhaustion
```

It added dead-network-source state tied to `sourceVersion`:

- existing progression watchdog and handoff grace remain;
- complete Blob fallback stays first priority;
- without a Blob, exhausted network Audio is marked dead;
- buffered data on a dead Audio cannot prove recovery;
- network recovery creates a fresh Audio;
- `online` may accelerate recovery but does not prove connectivity;
- rejected recovery `play()` preserves recovery intent;
- explicit Play does not reuse known-dead Audio;
- only real progression confirms success.

Status: **VERIFIED ON REAL STAGING** in two separate full-exhaustion tests. Both reached:

```text
recovery-play-resolved → real-progression-restored
```

Observed progression:

```text
36.290348 → 36.689745
34.258623 → 34.67215
```

In one test, source versions 2/3/4 were recreated while connectivity was still unusable; a later source loaded metadata and recovered. This was expected and safe: stale listeners/operations were invalidated and recovery intent survived.

### Retry accounting cleanup — `609dac1`

Diagnostics exposed `retryAttempt: 408`. It was not 408 executed retries: the counter incremented when a timer was scheduled, but valid cancellation did not consume/reset it.

```text
609dac1d12e4ce28575d31da5bb056e8b6e36c03
fix: count only executed MP3 recovery retries
```

New semantics:

- cancelled scheduled timers do not consume attempts;
- stale/guard-rejected callbacks do not consume attempts;
- only an accepted recovery attempt increments the counter;
- first real retry remains 15 seconds;
- backoff remains 15 → 30 → 60 → 120 seconds, capped at 120 ± jitter;
- real progression/`clearNetworkRecovery()` still resets it;
- dead-source, Blob, and ambient behavior are unchanged.

## 5. Final control test on `609dac1`

Status: **VERIFIED ON REAL STAGING**.

Trace:

```text
recovery-target-set
dead-source-confirmed
freeze-detected
online-received
old-audio-destroyed
new-audio-created
metadata-loaded
seek-restored
startup-buffer-ready
recovery-play-requested
recovery-play-resolved
real-progression-restored
```

Position was restored from `34.258564`; genuine progression appeared at `34.692164`. `online` accelerated recovery before the timer fired, while actual media progression proved success.

Retry accounting was correct: cancelled startup timers continued to show upcoming `retryAttempt: 1` without consuming it, and no runaway 408-style value appeared.

## 6. Shared music session cache and playback-priority milestone

Status: **VERIFIED ON REAL STAGING** at `a1b60a348ebbdf1d7b9f5c94321c91bc01c3df5`.

### Relax shared-cache test — PASS

```text
/music/relax/relax-mix-01.mp3
28,997,090 bytes
```

Observed:

- full Fetch completed and `cache-put` reported `stored: true`;
- switching away disposed the old `Mp3Engine`, while the shared `MusicSessionCache` retained the complete raw Blob;
- switching back produced `cache-get hit: true` and `engine-cache-restore hit: true`;
- with internet disabled, Relax played from the restored Blob;
- no audible interruption was observed while full Fetch/cache preparation was running.

Conclusion: Relax shared session cache plus offline restoration is **VERIFIED ON REAL STAGING**.

### Divnitsa A+B shared-cache test — PASS

```text
/music/divnitsa/divnitsa-mix-01.mp3 — 29,605,221 bytes
/music/divnitsa/divnitsa-mix-02.mp3 — 33,013,260 bytes

entryCount: 2
sizeBytes: 62,618,481
```

Observed:

- both tracks completed full Fetch and produced `cache-put stored: true`;
- the shared cache survived engine disposal after switching away;
- with internet disabled, switching back restored both tracks with `cache-get hit: true` and `engine-cache-restore hit: true`;
- Divnitsa played offline.

Conclusion: Divnitsa A+B shared session cache plus offline restoration is **VERIFIED ON REAL STAGING**.

### Playback-priority fix — PASS

```text
a1b60a348ebbdf1d7b9f5c94321c91bc01c3df5
fix: prioritize music playback over blob caching
```

Real staging showed that background full-Blob caching can complete without interrupting audible playback on a healthy connection. The governing priority remains:

```text
audible playback continuity > background Blob caching
```

Do not weaken the current cache-admission, yield, or cooldown safeguards. On a weak connection it is acceptable for complete Blob preparation to be postponed or aborted.

### Network observations

- Problematic Wi-Fi averaged approximately 46 KB/s over 15 seconds, with large fluctuations and dips below the throughput required for stable 128 kbps real-time playback.
- Phone hotspot averaged approximately 180 KB/s over 15 seconds, with substantially better sustained transfer.

This explains earlier cache/playback instability on the weak Wi-Fi path. Do not redesign SoundSpa around that pathological connection; background caching should yield or remain disabled whenever it might threaten audible playback.

### Browser HTTP cache caveat

A page reload destroys the JavaScript in-memory `MusicSessionCache`, while the browser HTTP cache may retain MP3 data. Offline playback after reload alone is therefore not proof that the shared session cache worked.

The accepted tests required explicit `cache-put stored: true`, `cache-get hit: true`, `engine-cache-restore hit: true`, and actual offline playback.

Production was not touched during these tests.

## 7. Current verified checkpoint

- Branch: `soundspa-v2`
- Verified staging code commit: `a1b60a348ebbdf1d7b9f5c94321c91bc01c3df5`
- MP3 full browser-buffer exhaustion recovery: **VERIFIED ON REAL STAGING**
- Retry-attempt accounting: **VERIFIED ON REAL STAGING**
- Ambient progressive playback plus Blob LRU: **VERIFIED ON REAL STAGING**
- Pause/A+B preparation lifecycle: **VERIFIED ON REAL STAGING**
- Relax real channel and Divnitsa/Relax switching: **VERIFIED ON REAL STAGING**
- Shared music session cache, Relax offline restore, and Divnitsa A+B offline restore: **VERIFIED ON REAL STAGING**
- Playback-priority safeguards: **VERIFIED ON REAL STAGING**
- Production: **UNCHANGED**

Always verify current local Git before relying on this expected state. Do not query remote/server state without authorization.

## 8. Key commits

- `7741cc2` — production stable rollback baseline.
- `85597c1` — initial v2 MP3/ambient baseline.
- `b9e5431` — isolated staging compose.
- `188ee17`, `c98ee9b`, `3361827` — staging nginx/ACME/HTTPS progression.
- `00e66e4` — resilient offline MP3 foundation.
- `cb532c2` — hybrid progressive plus Blob cache.
- `8416186` — adaptive startup buffering and buffer health.
- `76dc20f` — A/B preparation may continue while paused.
- `7a30ffb` — progressive ambient plus maximum-three Blob LRU.
- `8734a63` — first exhaustion fix; **failed staging verification**.
- `9ff2ae2` — recovery diagnostics.
- `d82e39f` — dead Audio recreation; passed two exhaustion tests.
- `609dac1` — executed-retry accounting; passed final control test.
- `a1d8748` — add Relax as the real second music channel using the same engine pattern.
- `4f74cba` — shared bounded session cache for complete raw music Blobs.
- `2898948` — shared-cache diagnostics.
- `73f8673` — detailed full-Fetch lifecycle diagnostics.
- `a1b60a3` — prioritize audible playback over Blob caching; passed shared-cache staging acceptance.

## 9. Stable decisions and limitations

Stable decisions:

1. Progressive audio starts before the full music Blob is ready.
2. Five buffered media-seconds gate startup; this is not a fixed wall-clock delay.
3. Genuine `currentTime` movement defines playback/recovery success.
4. Audible traffic outranks cache traffic.
5. Music uses a shared on-demand session cache for complete raw Blobs plus two active-engine A/B references; ambient uses a separate selected-track LRU capped at three.
6. Logical playlist target stays separate from audible fallback.
7. Complete local media outranks silence while testing network recovery.
8. Confirmed-dead network Audio is recreated.
9. `navigator.onLine` is only an acceleration hint.
10. Production isolation is non-negotiable.
11. Audible playback continuity always has priority over background full-Blob caching.
12. Ordinary music channels reuse the channel/playlist configuration and existing engine/cache rather than duplicating them.

Known limitations/deferred work:

- Blob caches are memory-only and disappear on reload/restart.
- Two ~30 MB music Blobs plus a replacement may create a significant memory peak.
- Broader Safari/long-duration coverage remains useful, but there is no open verified exhaustion bug.
- No dedicated offline/recovery UI status exists yet.
- Device auth, entitlements, subscriptions, protected delivery, and persistent offline storage are deferred.
- No Service Worker, IndexedDB, MediaSource, HLS/DASH, crossfade, DRM, or heartbeat has been added.

## 10. Safety rules

- Do not touch `/var/www/soundspa`, production containers/nginx/DB, or live traffic.
- Do not access Timeweb/staging unless explicitly requested.
- Do not change `mp3Engine.ts`, recovery timings, startup threshold, Blob A/B, or diagnostics without a concrete approved task.
- Do not modify `ambientEngine.ts` as part of unrelated music work.
- Do not put music media into Git.
- Never treat `online`, `canplay`, `playing`, buffered data, or resolved `play()` as definitive recovery success.
- Do not weaken the staging-verified playback-priority safeguards merely to make cache preparation finish sooner.

## 11. Current next step

The MP3 full-exhaustion recovery task and the shared music session-cache/playback-priority milestone are **CLOSED AND VERIFIED FOR STAGING**.

The next intended product task is adding a third real music channel. Reuse the existing channel/playlist configuration pattern used by Divnitsa and Relax. This should not require redesigning `mp3Engine.ts` or `MusicSessionCache`.

Production remains untouched until an explicit migration/deployment decision.

## 12. Instructions for a fresh AI session

1. Read this checkpoint completely.
2. Verify branch, HEAD, status, and diff locally.
3. Treat `a1b60a348ebbdf1d7b9f5c94321c91bc01c3df5` as the latest staging-verified code checkpoint, not proof of current state until Git is checked.
4. Preserve unrelated changes; never clean/reset without approval.
5. Treat `8734a63` as failed, `9ff2ae2` as diagnostics, `d82e39f` as the verified core fix, and `609dac1` as verified accounting cleanup.
6. Preserve the invariant that only real `currentTime` progression confirms recovery.
7. Consider exhaustion recovery and shared-cache/playback-priority acceptance closed unless new real evidence reopens them.
8. The intended next product task is a third real music channel using the existing configuration pattern, without redesigning the engine/cache.
9. Do not commit, push, deploy, or access staging/production without explicit permission.

## 13. Documentation note

`docs/SoundSpa 2 — Roadmap & Architecture.md` and `docs/Soundspa V2 plan_03_09_2026.md` are useful long-term/historical context but may describe older Blob-first behavior. They do not override this verified operational checkpoint. Update them only through a separately approved documentation task.
